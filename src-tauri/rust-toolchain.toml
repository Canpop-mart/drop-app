[toolchain]
# Pinned to a known-good nightly. Bumping this date is safe to try, but
# verify the build before merging — recent nightlies have been tightening
# the borrow checker around `Pin<&mut Self>` field borrows in ways that
# break droplet-rs's archive_backend.rs (E0502 + E0597 lifetime errors
# in fn poll_read).
#
# rc.4 (commit 48a4b0c) was the last release-tagged build that succeeded
# on this codebase; CI ran it on 2026-04-25, so its nightly was
# 2026-04-25 or earlier. 2026-04-20 gives a small buffer.
channel = "nightly-2026-04-20"
